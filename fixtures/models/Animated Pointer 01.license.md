# Лицензия: Animated Pointer 01.glb

> Sidecar-файл лицензии модели golden-корпуса. Заполнен вручную: встроенной лицензии
> (`asset.copyright`, `asset.extras`) в файле нет.
> Проверено: 2026-08-14.

- **Файл модели:** Animated Pointer 01.glb
- **Автор:** проект Tanyra3D
- **Лицензия:** Apache-2.0 (как весь репозиторий)
- **Copyright (asset.copyright):** _(в файле отсутствует)_
- **Источник / URL:** Сгенерировано кодом проекта (`_work/make-animation-pointer-fixture.mjs`),
  внешнего источника нет
- **Generator:** Tanyra3D fixture (animation pointer probe)

## Условия использования

- **Можно ли распространять (коммитить бинарник):** да
- **Требуется атрибуция:** нет

## Зачем эта модель в корпусе

Несёт `KHR_animation_pointer` с каналом, который **действительно работает**: цвет
материала едет от красного к синему за секунду.

Этим она отличается от `Unknown Ext Pointer 01.glb`. Та только объявляет расширение,
анимаций внутри нет вовсе, и отвечает она на один вопрос — переживёт ли объявление
проход оптимизатора. Проверить на ней показ невозможно: проигрывать нечего.

Указатель адресует место в JSON (`/materials/0/pbrMetallicRoughness/baseColorFactor`), а
не узел сцены. Именно поэтому загрузчик three.js без плагина выбрасывает такой канал
молча — `if (target.node === undefined) continue` — и модель приезжает с пустым клипом,
снаружи неотличимая от модели без анимации.

Модели с рабочей анимацией в корпусе были только чужие: персонажи и образцы Khronos, ни
одна в git не едет. Значит любое утверждение про анимацию проверялось только на машине
Александра, а на чистом клоне и на CI пропускалось молча. Эта — полтора килобайта,
Apache-2.0, работает везде.

Валидатор Khronos: 0 ошибок, 0 предупреждений.
