# зависимости и артефакты — не версионируем
node_modules/
logs/

# модели: вход и результат — большие бинарники, версионируем только код
input/
output/

# рабочая папка для временных файлов/бэкапов (не код)
_work/

# веб-интерфейс: загрузки и результаты обработки (большие бинарники, не код)
_web/

# Freebuff Desktop — база данных сессии (авто-генерируемая)
.freebuff/

# диагностика
diag_report.txt
vitest-report.json

# локальные настройки Claude Code: список разрешений конкретной машины и её пути.
# В публичном репозитории это мусор — и он там уже полежал, с путями к диску E:,
# которого давно нет. Общие настройки проекта — в .claude/settings.json, они версионируются.
.claude/settings.local.json

# личные заметки-шпаргалки в корне
Текстовый документ.txt
