# Словарь формулировок — технический термин → человеческий язык

> Справочник для текстов интерфейса и отчёта. Принцип: интерфейс раскрывает намерения, а не
> алгоритмы. Технические детали доступны на 2-м уровне progressive disclosure
> (ARCHITECTURE §4c), но по умолчанию пользователь видит человеческую формулировку.
>
> *Ревизия 2026-09-01: раньше здесь стояло «для `web-interface` и `ai-assistant`» — имена
> агентов-исполнителей, отменённых Правилом 3. Границы слоёв остались, отдельных
> исполнителей нет, и читатель искал бы их напрасно.*
>
> Правило: НЕ пугать терминологией. Уровень 1 объяснения — на языке художника, не инженера.

---

## Общий принцип

| Вместо (инженерное) | Показывать (человеческое) |
|---|---|
| Aggressive mesh simplification | Рекомендуется |
| GPU Texture Allocation | Использование памяти |
| Reduce textures | Ваши текстуры 4096×4096 — для мобильной цели это ~64 МБ памяти. Уменьшить до 2048×2048? |
| Merge vertices | Найдено 321 дублированная вершина — их удаление снижает нагрузку на GPU |
| Vertex welding | Объединение совпадающих точек модели |
| Draco compression | Сжатие геометрии (нужен декодер на стороне движка — см. отчёт) |
| KTX2 / Basis | Сжатие текстур под видеопамять |
| Draw calls | Количество отдельных отрисовок за кадр |
| Triangulation | Разбиение граней на треугольники (нужно движкам) |

---

## Правила словаря

- Число + следствие вместо голого действия: не «уменьшить текстуры», а «4096→2048, экономия 75% памяти, на глаз незаметно».
- Слово «Рекомендуется» вместо названий агрессивных операций — сила решения прячется во 2-й уровень.
- Единицы — человеческие: МБ, пиксели, «штуки», а не байты и внутренние коды.
- Термин допустим, если рядом сразу его смысл в скобках (один раз), дальше можно термином.

---

## Что переводить, а что оставить английским (на решение Александра)

Ориентир — Blender: русскими становятся обычные слова, названия технологий остаются как
есть. Художник, который ищет ответ в интернете, должен искать по тому же слову, что видит
у нас. Переведённый термин ломает ровно это.

Три класса, и правило для каждого разное:

1. **Названия технологий — не переводить никогда.** Draco, Meshopt, KTX2, UASTC, ETC1S,
   glTF. Это имена собственные, и именно по ним человек ищет ответ в интернете.

   **Но имя технологии ≠ идентификатор расширения.** `EXT_mesh_gpu_instancing`,
   `EXT_texture_webp`, `KHR_texture_basisu`, а с ними имена загрузчиков (`KTX2Loader`,
   `basisu`) в подсказках для пользователя **не показываются вообще** — ни в скобках,
   ни без. Это не то слово, по которому ищут; человек видит его в метаданных и в отчёте
   валидатора, и этого достаточно. Решение Александра 2026-08-01.
2. **Обычные слова — переводить.** «удалено», «объединено», «не применено», «рекомендуется».
3. **Термины ремесла — спорная зона.** Ниже таблица: что сейчас выбрано и что стоит
   обсудить. Колонка «решение» — под правку Александром.

   *Ревизия 2026-09-01 сверила таблицу с каталогами.* Четыре строки числились открытыми, а
   решение по ним уже было принято В КОДЕ и там же сослалось на этот словарь как на
   источник — то есть вопрос ходил по кругу. Они закрыты записью факта, а не новым
   решением: `draw calls`, `vertex weld`, `morph target` и `joints` (последней строки в
   таблице не было вовсе). Открытыми остались шесть.

| Термин | Сейчас в интерфейсе | Альтернатива | Решение |
|---|---|---|---|
| mesh | **меш** | сетка / модель | принято 2026-09-01, слово Александра: «меш и узел оставляем» |
| draw calls | **отрисовки за кадр** | draw calls | принято: шапка `addons/gltf/messages/ru.mjs` называет это решением по этому же словарю |
| vertex weld | **совпадающие вершины склеены** | weld | принято: там же, в шапке каталога |
| accessor | **accessor** | блок данных | принято 2026-09-01, слово Александра: «accessor и bounding box оставляем английским». Было «блок данных» — три строки отчёта переписаны. Довод: по этому слову ищут в спецификации |
| skin | **скин** | скиннинг / привязка | ? |
| morph target | **запасные формы** | морф / ключ формы | принято 2026-08 в коде (`rule.sceneMorphTargets`): «слово из спецификации новичку не говорит ничего, а форма — говорит». Ревизия 2026-09-01 перенесла решение сюда: таблица числила вопрос открытым и попросила бы решить уже решённое |
| bounding box | **bounding box** | габаритный бокс | принято 2026-09-01, тем же словом Александра. Уже был английским — менять нечего |
| node | **узел** | нода | принято 2026-09-01, слово Александра: «меш и узел оставляем» |
| primitive | **примитив** | — | ? |
| joints | **кости** | джойнты | принято: «то самое слово, которым их зовут в любом 3D-редакторе» (`ru.mjs`, раздел скина). Строки в таблице не было — добавлена ревизией 2026-09-01 |
| degenerate triangle | **вырожденный треугольник** | — | принято |
| orphan vertex | **висящая вершина** | — | принято |
| prune | **чистка** | — | принято |
| dedup | **объединение одинаковых** | — | принято |

Проверка на каждый спорный случай: **по какому слову человек будет искать в интернете,
когда захочет разобраться?** Если по английскому — оставляем английское.

## Связь

- ARCHITECTURE.md §4c — progressive disclosure (где какой уровень показывается)
- `docs/ПРАВИЛА_ИНТЕРФЕЙСА.md` — постоянные правила экрана (заведён 2026-08-26). Словарь
  отвечает на вопрос «какими СЛОВАМИ», тот документ — «где и сколько раз». Ссылка добавлена
  ревизией 2026-09-01: документы соседние, а знали друг о друге только в одну сторону.
- `CLAUDE.md`, Правило 10 — подсказка у опции отвечает на два вопроса и не называет
  идентификаторов расширений. Словарь этому не противоречит и не повторяет его.
