# Что приложение умеет сегодня

> Плоский список возможностей на текущий момент. Не журнал работ, не планы,
> не позиционирование (это `README.md`).
>
> Отвечает ровно на один вопрос: **«у нас это уже есть?»** — чтобы не искать ответ в коде
> и не спрашивать заново. Версия 0.2.0.
>
> **Обновлять при каждой новой возможности.** Строка устаревает молча, а верят ей как
> факту — это хуже, чем её отсутствие.

## Обработка

| Возможность | Есть | Как называется / чем делается |
|---|:--:|---|
| Безопасная чистка (дубликаты, мусор, сварка вершин, вырожденные треугольники) | ✓ | опция `safe` |
| Сжатие геометрии Draco | ✓ | `draco` |
| Сжатие геометрии Meshopt | ✓ | `meshopt` |
| Квантование геометрии | ✓ | `quantize`, `KHR_mesh_quantization` — единственный способ уменьшить геометрию **без декодера на сайте**; взаимоисключающий с Draco и Meshopt (те квантуют сами) |
| Сжатие текстур в KTX2 | ✓ | `ktx2`, режимы UASTC и ETC1S |
| Сжатие текстур в WebP | ✓ | `webp` — меньше файл при неизменной видеопамяти; потяжелевшая текстура возвращается к исходнику |
| Объединение мешей | ✓ | `join` |
| GPU-инстансинг повторяющихся мешей | ✓ | `instance`, `EXT_mesh_gpu_instancing`; порог — 2 узла на меш |
| Авто-выбор инстансинга, когда в модели общая геометрия | ✓ | значок «Советуем» (не путать с «В модели»: там технология уже в файле, здесь — ещё нет) |
| Умное объединение мешей: общая геометрия не размножается | ✓ | `join` объединяет только меши с одним владельцем; общие оставляет и сообщает об этом (`join.keptShared`) |
| Отказ структурных правил на неизвестном расширении | ✓ | дедупликация, чистка, объединение и инстансинг не работают, если файл объявляет расширение, которого не знает библиотека (`KHR_animation_pointer` и подобные адресуют свойства по номеру) |
| Прореживание ключей анимации | ✓ | `resample` |
| Удаление вершинных цветов | ✓ | `strip-colors` |
| Всё выключено по умолчанию (passthrough) | ✓ | `advancedFeatures: []` |
| Проверка целостности результата | ✓ | сверка с контрольной точкой; при расхождении файл всё равно пишется, но помечается красным — решение за пользователем |
| Упаковка ORM (AO + roughness + metalness в одну текстуру) | ✗ | правила нет |
| Уменьшение разрешения текстур | ✗ | после 0.1.0 |
| Генерация LOD | ✗ | после 0.1.0 |
| Упрощение сетки (decimation) | ✗ | правила нет |
| Конвертация в USDZ и другие форматы | ✗ | только glTF/GLB |

Всего правил обработки — 12, в `addons/gltf/rules.mjs`.

## Просмотр

| Возможность | Есть |
|---|:--:|
| Два вьюпорта рядом: исходник ⇄ результат | ✓ |
| Связанные камеры с идентичными near/far и пределами приближения | ✓ |
| Проигрывание анимации, общее время на оба вьюпорта | ✓ |
| Выбор клипа анимации, перемотка | ✓ |
| Ползунок экспозиции, общий на оба вьюпорта | ✓ |
| Панель анимации скрыта, если клипов нет | ✓ |
| Живой замер нагрузки на отрисовку в каждом вьюпорте | ✓ (миллисекунды на кадр, медиана по 60 кадрам, и во сколько раз результат легче исходника) |
| Отдельный FPS для левого и правого окна | ✗ — и не будет: оба вьюпорта рисуются в одном кадре, счётчик дал бы два одинаковых числа |
| Второй движок рендера (не three.js) | ✗ |

## Отчёт и интерфейс

| Возможность | Есть |
|---|:--:|
| Метаданные и отчёт валидатора Khronos по обеим моделям | ✓ |
| Схлопывание повторяющихся строк отчёта | ✓ |
| Отметка необратимых изменений | ✓ |
| Проверка бюджета платформы | ✓ (треугольники, материалы, размер файла — там, где есть источник) |
| Проверка числа материалов | ✓ |
| Проверка размера текстур | ✗ (порог есть, но ядро не отдаёт размерность текстур) |
| Цветовая оценка показателей по бюджету платформы | ✓ (жёлтый — рекомендация, красный — жёсткий предел площадки) |
| Ссылка на документ-источник у каждого порога | ✓ |
| Интерфейс на русском и английском | ✓ |
| Язык по умолчанию — английский, выбор запоминается | ✓ |
| Смена языка ничего не сбрасывает | ✓ (флажки, кодек, значки, отчёт, скачивание, список моделей остаются как были; модель не перезаливается и не пересобирается) |
| Отчёт уже собранной модели переводится без пересборки | ✓ (записи несут `messageId` + `data`, строки собираются заново — `localizeResult`) |
| Тексты отчёта на русском (итог, план, описания опций) | ✓ |
| Строки правил обработки на русском | ✓ (каталог `addons/gltf/messages/ru.mjs`; у чужого аддона перевода может не быть — тогда английский) |
| Проверка целостности и названия правил на русском | ✓ |
| Сообщения валидатора Khronos на русском | ✓ (все 169 кодов из 169) |
| Добавление языка без правок кода | ✓ (два файла в `translations/`, см. `ui/locales/README.md`) |
| Верхнее меню (Файл / Настройки / Справка) | ✓ |
| Выбор режима флажков: следовать подсказкам ⇄ оставлять мой выбор | ✓ (Настройки; по умолчанию — подсказки, хранится в браузере) |
| Проценты изменения у размера файла и видеопамяти | ✓ (только у них: у остальных метрик процент — шум; точность по величине — «−0.06 %», а не округлённый ноль) |
| Общий вердикт одним числом над вьюпортом | ✗ — убран намеренно: считался по одному размеру файла и объявлял ухудшением размен, где пять других строк улучшились |
| Красный знак у галочки, которая сильно увеличила результат | ✓ (`!` с числами и советом; цену меряет само правило, а не догадывается интерфейс) |
| Красный знак у модели, с которой что-то не так | ✓ (файл не читается или нарушает стандарт glTF — `!` в списке моделей и на кнопках «Метаданные» и «Проверка») |
| Единая семья значков-предупреждений | ✓ (контурный треугольник с «?» — нужен декодер на площадке; контурный круг с «!» — проблема. 📖 в семью не входит: это документация) |
| Несколько моделей в списке, одна в сцене | ✓ (переключение без перезаливки, результат каждой сохраняется) |
| Обратная связь из приложения | ✗ |
| Индикатор ожидания во вьюпорте (загрузка слева, сборка справа) | ✓ (подбрасываемая кость сверху по центру, поверх прежней модели) |
| Экспорт в GLB и glTF JSON | ✓ |

## Движки и площадки

Две разные оси (`docs/ARCHITECTURE.md` §4g): движок — чем модель РИСУЕТСЯ,
площадка — КУДА она уезжает. Оба — данные (`engines/*.json`, `profiles/*.json`),
добавление не требует правки кода.

| Движок | Состояние |
|---|---|
| Three.js | включён |
| model-viewer | включён (2026-08-10) |
| Babylon.js | 0.3.0, см. `ROADMAP.md` §5g |
| PlayCanvas | не написан, технически подходит |
| Unity, Unreal | не будет — если понадобится, сообщество сделает профиль само |

| Площадка | Состояние |
|---|---|
| Прочерк («без площадки») | выбор по умолчанию; пороги общевебовые, из Khronos glTF Asset Auditor |
| Shopify | включена (2026-08-10, движок model-viewer) |
| Mobile | написана, `enabled: false` |
| Meta Quest | написана, `enabled: false` |

**Порогов без источника в профилях нет.** Неподтверждённые убраны, оставшиеся имеют поле
`source` со ссылкой на документ, и ссылка показывается пользователю. Метрика без порога не
исчезает — значение измеряется и показывается, оценка не выносится. Разбор:
`docs/БЮДЖЕТЫ_источники.md`.

## Установка и распространение

| | Есть |
|---|:--:|
| Запуск локально (`node server.mjs`) | ✓ |
| Установщик для Windows / macOS / Linux | ✓ (собирает CI по тегу, `.github/workflows/release.yml`; Windows проверен установкой, macOS/Linux на живой машине не запускались) |
| Раздача через winget / brew | ✗ |
| Нода n8n | ✗ (0.1.2) |
| Отправка отчётов о падениях | ✗ (после 0.1.0) |
