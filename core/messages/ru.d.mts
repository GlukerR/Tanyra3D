// core/messages/ru.d.mts — форма каталога для TypeScript. Причины и оговорки — те же,
// что и у соседнего en.d.mts: каталог остаётся на JavaScript, потому что его правит
// переводчик, а собранный файл затирается при каждой сборке.

import type { MessageCatalog } from '../types.mjs';

declare const messages: MessageCatalog;
export default messages;
