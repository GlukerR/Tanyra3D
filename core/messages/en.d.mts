// core/messages/en.d.mts — форма каталога для TypeScript. Сам каталог остаётся на
// JavaScript (en.mjs) и правится переводчиком напрямую: сделать его собранным из .mts
// значит подставить переводчика — его правка попала бы в файл, который затрёт
// следующая сборка. Здесь объявлена только форма значения, текста тут нет и не будет.
//
// Файл рукописный, в отличие от core/*.d.mts рядом с переведёнными модулями: те
// генерируются и в git не идут, этот — идёт.

import type { MessageCatalog } from '../types.mjs';

declare const messages: MessageCatalog;
export default messages;
